import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the AddressSearchBlock component
 */
export const AddressSearchBlockStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(AddressSearchBlockStyles, {
    container: {
        fontSize: 22
    }
})

