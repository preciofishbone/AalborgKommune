import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the AddressSearchBlockSettings component
 */
export const AddressSearchBlockSettingsStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(AddressSearchBlockSettingsStyles, {
    container: {
        fontSize: 22
    }
})

